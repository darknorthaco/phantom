# Phantom Stone-Home / Tauri deployer — Python runtime (matches phantom_deployer::install_python_deps).
# Used for offline wheelhouse generation (installer/offline_bundle.py).
# Broader dev stack: see ../phantom_core/requirements.txt

fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
httpx>=0.25.0
requests>=2.31.0
websockets>=11.0.0
psutil>=5.9.0
numpy>=1.24.0
pyyaml>=6.0
cryptography>=41.0.0
pyjwt>=2.8.0
