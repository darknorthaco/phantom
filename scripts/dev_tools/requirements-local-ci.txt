# Minimal deps for Local CI (mirrors .github/workflows/build.yml test-controller + smoke).
# Installed only into Phantom's venv via local_ci_check.py --ensure-dev-tools.
# Intentionally omits torch/transformers to keep local runs fast; matches CI test-controller job.

fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
httpx>=0.25.0
requests>=2.31.0
websockets>=11.0.0
psutil>=5.9.0
numpy>=1.24.0
pyyaml>=6.0
cryptography>=41.0.0
pyjwt>=2.8.0
pytest>=7.4.0
pytest-asyncio>=0.21.0
black>=23.0.0
flake8>=6.0.0
